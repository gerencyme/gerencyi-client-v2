describe("AuthCard", () => {
  it("should render a authentication card", () => {});

  it("should render a register card", () => {});

  it("should be able to change method of card", () => {});
});
