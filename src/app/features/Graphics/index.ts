import { GraphicContent } from '@features/Graphics/views/GraphicContent'
import { GraphicsActions } from '@features/Graphics/views/GraphicsActions'
import { GraphicsHeader } from '@features/Graphics/views/GraphicsHeader'
import { GraphicsRoot } from '@features/Graphics/views/GraphicsRoot'

export const Graphics = {
  root: GraphicsRoot,
  header: GraphicsHeader,
  content: GraphicContent,
  actions: GraphicsActions,
}
